import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Join a workspace | Calibrate",
};

export default function InviteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
