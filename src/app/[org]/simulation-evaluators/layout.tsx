import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Simulation evaluators | Calibrate",
};

export default function SimulationEvaluatorsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
