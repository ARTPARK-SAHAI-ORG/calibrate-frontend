import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Agent evaluators | Calibrate",
};

export default function AgentEvaluatorsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
