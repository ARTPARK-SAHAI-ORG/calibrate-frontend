NEXT_PUBLIC_BACKEND_URL=

# NextAuth.js Configuration
# Generate a secret using: openssl rand -base64 32
AUTH_SECRET=

# Google OAuth Credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: Your app URL (required for production)
# AUTH_URL=https://yourdomain.com

# Maintenance Mode: Set to "true" to show a maintenance page
# MAINTENANCE_MODE=true

# Google Analytics Measurement ID (starts with G-)
NEXT_PUBLIC_GA_MEASUREMENT_ID=

NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development

# The full address this build answers on. Without it the app treats itself as a
# copy of the hosted site and stays out of search results.
NEXT_PUBLIC_APP_URL=http://localhost:3000